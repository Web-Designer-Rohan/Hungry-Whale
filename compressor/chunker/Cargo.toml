[package]
name = "hungry-chunker"
version = "0.1.0"
edition = "2021"
description = "FastCDC chunking + zstd compression + cross-file chunk dedupe into a binary container"

[dependencies]
fastcdc = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
zstd = "0.13"
